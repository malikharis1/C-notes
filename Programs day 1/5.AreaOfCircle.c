// Area of circle 

#include <stdio.h>
#include <conio.h>
void main()
{
   int r, formula;
   printf("Enter the radius of a circle :");
   scanf("%d", &r);
   formula = 3.14 * r * r;
   printf("The radius of Circle is : %d", formula);
}